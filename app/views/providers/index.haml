%p#notice= notice
%h1 Providers
%table.table.table-sm.table-hover
  %thead.thead-inverse
    %tr
      %th Name
      %th{:colspan => "3"}
  %tbody
    - @providers.each do |provider|
      %tr
        %td= provider.name
        %td{ style: 'width: 1%; white-space: nowrap;' }
          = link_to 'Edit', edit_provider_path(provider), class: 'btn btn-warning'
          = link_to 'Destroy', provider, method: :delete, data: { confirm: 'Are you sure?' }, class: 'btn btn-danger'
%br/
= link_to 'New Provider', new_provider_path, class: 'btn btn-primary'
