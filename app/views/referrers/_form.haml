= form_with(model: referrer, local: true) do |form|
  - if referrer.errors.any?
    #error_explanation
      %h2
        = pluralize(referrer.errors.count, "error")
        prohibited this referrer from being saved:
      %ul
        - referrer.errors.full_messages.each do |message|
          %li= message
  .field
    = form.label :name
    = form.text_field :name, id: :referrer_name
  .field
    = form.label :zorder
    = form.text_field :zorder, id: :zorder_name
  .actions
    = form.submit
